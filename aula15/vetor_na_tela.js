/*
let valores = [9, 5, 6, 1, 6]
for (let pos = 0; pos < valores.length; pos++) {
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}
*/
let num = [2,6,8,1,3]
for (let pos in num) {
    console.log(`A posição ${pos} tem o valor ${num[pos]}`)
}